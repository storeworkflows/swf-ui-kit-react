import React from 'react';

import { default as Modal } from './Modal';

export default {
  title: 'swf-ui-kit/Containers/Modal',
  component: Modal,
};

const Template = (args) => <Modal {...args} />;

export const Standard = Template.bind({});
